spring.application.name=kharidoMat

# --- Cloud MySQL (Railway) ---
spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}
spring.datasource.username=${MYSQLUSER}
spring.datasource.password=${MYSQLROOT_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# --- Mail settings (keep as-is) ---
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sujaljain004@gmail.com
spring.mail.password=rtzl bqsz iniq uxek
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000
spring.mail.properties.mail.debug=true

# --- Profiles ---
spring.profiles.active=prod

# --- Server port (Railway sets PORT automatically) ---
server.port=${PORT:8080}

# --- Razorpay keys ---
razorpay.key_id=rzp_test_n5Y0q2oWkbhx2b
razorpay.key_secret=SJav5iDCiTd3x43xN3U4ru1Z
